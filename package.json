{
  "name": "functional-login",
  "version": "1.0.0",
  "description": "A functional login component using JWT that creates a user in a mongodb",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js --ignore client",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hope428/functional-login.git"
  },
  "author": "Leon Forsythe",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hope428/functional-login/issues"
  },
  "homepage": "https://github.com/hope428/functional-login#readme",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
